---
microservice-util-lib: patch
---

- Improved error handling for non-retryable responses: The retry middleware now consistently throws errors for all non-ok HTTP responses (status codes outside the 200-299 range) that don't meet retry conditions.
