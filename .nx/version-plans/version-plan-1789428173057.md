---
appbuilder-util-lib: patch
aws-wrappers: patch
create-workspace: patch
lambda-test-utils: patch
microservice-util-lib: patch
nx-appbuilder: patch
nx-openapi: patch
nx-cdk: patch
vite-plugin-handler: patch
---

- MI-331: Cross-reference @aligent/aws-wrappers createMockService from lambda-test-utils docs
- Fix `tsc` emitting only `.d.ts` (no `.js`) for packages still built via the plain `@nx/js:tsc`
  executor. `@aligent/ts-code-standards`'s `tsconfigs-extend` (≥4.2.0) sets
  `emitDeclarationOnly: true`, assuming a bundler does the JS emit — packages that don't bundle
  need it overridden back off in `tsconfig.lib.json`.
- Cleanup tsconfigs for packages that are built by `@nx/rollup` executor.
