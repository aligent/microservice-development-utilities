---
microservice-util-lib: minor
---

MI-331: Add optional `isBody` runtime type-guard parameter to `isHttpResponseError` so callers can validate the response body shape at runtime, instead of relying solely on the `TBody` compile-time type parameter. Backward compatible — omitting `isBody` preserves prior behaviour.
