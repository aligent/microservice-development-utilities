---
microservice-util-lib: minor
vite-plugin-handler: minor
---

microservice-util-lib:
- Add `parseXmlResponse` middleware to convert XML responses to JSON for `openapi-fetch` clients
- Add `requestTimeout` middleware to abort requests after a configurable duration
- Include `contentType` in log middleware response output
- Fix body parser to return `null` for empty bodies and fall back to text for unknown content types

vite-plugin-handler:
- Remove `stripUnneededPlugins` in favour of disabling `pluginTimings` via rolldown checks
