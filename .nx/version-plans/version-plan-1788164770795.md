---
vite-plugin-handler: minor
---

MI-331: Add `quiet` option to `handlerBundle` to reduce noisy build output

- Installs a `customLogger` that filters Vite/Rolldown's per-module and per-chunk progress lines (`transforming...`, `✓ N modules transformed.`, `rendering chunks...`, `computing gzip size...`) for handler environments, keeping only the build-start line, the size/gzip summary line, and the `✓ built in ...` line
- Enabled by default (`quiet: true`); pass `quiet: false` to restore full build output
- Wraps any existing `customLogger` set in the consumer's `vite.config` instead of replacing it, and leaves warnings, errors, and non-handler environment output untouched
