---
nx-openapi: major
---

# Breaking changes
- Clients are now generated in the same Nx project, rather than having a separate project per api client. 
- All clients are then exported by the '@clients'.
