{
    "$schema": "http://json-schema.org/schema",
    "$id": "PackageGenerator",
    "title": "Package Generator",
    "type": "object",
    "description": "Generate a new package in the monorepo",
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the package (without @aligent/ scope)",
            "x-prompt": "What is the name of the package?",
            "pattern": "^[a-z][a-z0-9-]*$",
            "patternErrorMessage": "Package name must start with a lowercase letter and contain only lowercase letters, numbers, and hyphens"
        },
        "description": {
            "type": "string",
            "description": "A brief description of the package",
            "x-prompt": "What is the description of the package?"
        }
    },
    "required": ["name", "description"]
}
