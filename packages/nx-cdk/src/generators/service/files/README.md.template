# <%= serviceName %>

This is a CDK service generated using `@aligent/nx-cdk` for Nx.

## Overview

<!-- Very brief explanation of what this service does -->

## Architecture

<!-- What systems this service integrates with and how -->

```mermaid
graph LR
    A[System A]
    INT[INT]
    B[System B]
    A-->INT-->B
```

## Technical Decisions

<!-- Why an approach was chosen over alternatives -->

## Infrastructure

<!-- What AWS resources the CDK stack provisions (queues, buckets, tables, etc.) -->

## Development

### Type Checking

```bash
yarn nx check-types <%= name %>
```

### Testing

```bash
yarn nx test <%= name %>
```

### Deploy to Playground

```bash
yarn pg:deploy dev/<%= name %>
```

## Configuration & Secrets

This service requires the following parameters to be present in AWS SSM Parameter Store or Secrets Manager **before deployment**.

## Deployment

This service is deployed as part of the CDK application. See the main README for deployment instructions.
