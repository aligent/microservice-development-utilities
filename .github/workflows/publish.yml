name: Publish to NPM

on:
  push:
    branches:
      - main
  # Run manually using Github UI
  workflow_dispatch:
    inputs:
      publish-options:
        description: 'Additional flags to pass to `nx release publish` command'
        required: false
        default: ''

jobs:
  build-and-publish:
    name: Build and publish
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: ${{ github.ref_name }}

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'
          package-manager-cache: false

      - name: Enable Corepack
        run: corepack enable

      - name: Configure Dependency Cache
        uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'
          cache: 'yarn'
          registry-url: https://registry.npmjs.org/

      - name: Install dependencies
        run: yarn install --immutable

      - name: 📦 Build packages
        run: yarn nx run-many -t build

      - name: 🚀 Publish versioned packages
        env:
          PUBLISH_OPTIONS: ${{ inputs.publish-options }}
        run: yarn nx release publish --verbose --access public $PUBLISH_OPTIONS
